For this question, consider all the ways that the class may be used. Is there a way to use the class that would result in an error?
```cpp
#include <iostream>
#include <vector>

using namespace std;

class Int_Holder {
private:
  int *p;
  int mult;

public:
  Int_Holder() : p(nullptr), mult(0) {};
  void init_p(int &i) { p = &i; }
  void init_mult(int m) { mult = m; }
  int get_mult() { return (*p) * mult; }
};
```

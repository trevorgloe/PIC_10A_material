For this problem, consider all the ways the given function could be called. Are some of these ways problematic the way the function is written?
```cpp
#include <iostream>
#include <vector>

using namespace std;

bool first_elem_prime(vector<int> &v) {
  int test = v[0];
  for (int i = 2; i < test; ++i) {
    if (test % i == 0) {
      return false;
    }
  }
  return true;
}
```

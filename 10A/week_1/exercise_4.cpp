#include<iostream>
#include<cmath>

/*
We have 2 vectors in 2d space. We want to compute the cosine between these vectors. The way that we would do that is to compute the inner product (x1*y1 + x2*y2) and divide by the norm of both the vectors, |x|^2 = x1^2 + x2^2

In the main function below, compute the cosine between the two vectors and print it
*/
using namespace std;

int main() {
    float x1 = 3.41, x2 = 8.2; // vector 1
    float y1 = -2.1, y2 = 4.21; // vector 2


    return 0;
}
